angular
    .module('osbb')
    .config(['$locationProvider', '$httpProvider', '$stateProvider', '$urlRouterProvider', cfg]);

function cfg($locationProvider, $httpProvider, $stateProvider, $urlRouterProvider) {
    //$stateProvider
    //    .state('register', {
    //        url: '/',
    //        controller: 'registerController as a'
    //    });
}